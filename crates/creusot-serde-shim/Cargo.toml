[package]
name = "creusot-serde-shim"
description = "Creusot-only stand-in for serde's Deserialize derive (issue #121); never compiled into a shipped build."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
publish = false

[lib]
proc-macro = true

[dependencies]
quote = "1"
syn = { version = "2", features = ["derive"] }
